export default () => {
    // this is the default chugger configuration file...

    return {
        server: {
            hostname: "0.0.0.0",
            port: 8080
        },
        watch: {
            src: '/'
        }
    }
};
